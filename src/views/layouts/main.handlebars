<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/chat.css" />
    <title>Turisteando</title>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-md">
            <div class="container-fluid">
                <a class="navbar-brand" href="/">Turisteando</a>
                <form class="d-flex my-2 my-lg-0 justify-content-right">
                    <ul class="navbar-nav">
                        {{#if user }}
                        <li class="nav-item">
                            <a class="nav-link" href="/products">Products</a>
                        </li>
                        {{/if}}
                    </ul>
                </form>
                <div class="d-flex">
                    {{#if user }}
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link" href="#">{{user}}</a>
                        </li>
                    </ul>
                    <div class="flex-shrink-0 dropdown">
                        <a href="#" class="d-block link-body-emphasis text-decoration-none dropdown-toggle show" data-bs-toggle="dropdown" aria-expanded="true">
                            <img src={{photo}} alt="mdo" width="32" height="32" class="rounded-circle">
                        </a>
                        <ul class="dropdown-menu text-small shadow" style="position: absolute; inset: 0px 0px auto auto; margin: 0px; transform: translate(0px, 34.4444px);" data-popper-placement="bottom-end">
                            <li><a class="dropdown-item" href="/api/auth/signout">Sign out</a></li>
                        </ul>
                    </div>
                    {{else}}
                    <ul class="navbar-nav">
                        <li>
                            <a class="nav-link" href="/register">Create account</a>
                        </li>
                    </ul>
                    {{/if}}
                </div>
            </div>
        </nav>
    </header>
    <main class="container">
        {{{body}}}
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>

</html>